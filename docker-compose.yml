version: '3.8'

services:
  # 1. PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: pmposhan-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Ayush@202325
      POSTGRES_DB: pmposhan
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    networks:
      - pmposhan-network

  # 2. FastAPI Backend
  backend:
    build: ./backend
    container_name: pmposhan-api
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgresql://postgres:Ayush%40202325@db:5432/pmposhan
      ALLOWED_ORIGINS: https://pmposhanyojnamdm.duckdns.org,http://pmposhanyojnamdm.duckdns.org,http://206.189.128.72,https://206.189.128.72
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    networks:
      - pmposhan-network

  # 3. React Frontend (Nginx with SSL)
  frontend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: pmposhan-ui
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    networks:
      - pmposhan-network

networks:
  pmposhan-network:
    driver: bridge

volumes:
  postgres_data:
